services:
  - type: web
    name: reclame-aqui-scraper
    runtime: python
    buildCommand: pip install -r requirements.txt
    startCommand: uvicorn src.api:app --host 0.0.0.0 --port $PORT
    envVars:
      - key: PYTHON_VERSION
        value: "3.11"
      - key: FIRECRAWL_API_KEY
        sync: false  # Você vai configurar manualmente no dashboard
